# 正则表达式匹配


class Solution:
    """
    1 a*  匹配多个a,string索引后移x位,直到与pattern *字符的下一位字符相等
    2 a.  string 索引后移一位,与pattern继续匹配
    3 .*  匹配任意,string索引后移x位,直到与pattern *字符的下一位字符相等
    """

    def isMatch(self, string: str, pattern: str) -> bool:
        pass
